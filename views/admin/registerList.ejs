<div class="col-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Ro'yhatdan o'tib junatilgan murojatlar</h4>
        </div>
        <div class="card-body">     
            <div class="table-responsive">
                <table id="example3" class="table table-responsive-md" style="min-width: 845px">
                    <thead>
                        <tr>
                            <th>Ism</th>
                            <th>Telefon raqam</th>
                            <th>Kurs vaqti</th>
                            <th>Sana</th>
                            <th>Tahrirlash</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for (let item of userAll) { %>
                        <tr> 
                            <td><%= item.name %></td>
                            <td><%= item.phone %></td>
                            <td><%= item.courseTime %></td>
                            <td><%= moment(item.date).format("DD-MM-YYYY") %></td>
                           <td> 
                                <div class="d-flex">
                                    <a href="/team/<%= item._id %>" class="btn btn-primary shadow btn-xs sharp me-1"><i class="fas fa-pencil-alt"></i></a>
                                    <a href="#" class="btn btn-primary shadow btn-xs sharp me-1"><i class="fas fa-file-alt"></i></a>
                                    
                                    <form method="POST" class="hidden-r" action="<%=`/team/delete/${item._id}?_method=DELETE`%>">
                                        <button type="submit" class="btn btn-danger shadow btn-xs sharp me-1">
                                          <i class="fa fa-trash"></i>
                                         </button>
                                      </form>
                                </div>												
                            </td>												
                        </tr>
                        <%}%>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>